import React from 'react';

import { StepEstimatedMinutesField } from '../StepEstimatedMinutesField';

/** An intro sets expectations, so a time estimate is all it configures. */
export const IntroStepFields: React.FC = () => <StepEstimatedMinutesField />;

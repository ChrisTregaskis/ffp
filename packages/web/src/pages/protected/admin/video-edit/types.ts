import type { VideoMetadataFormValues } from '@web/components/form/videos/types';

/** Form values for the video edit form — alias for VideoMetadataFormValues (all fields included) */
export type VideoEditFormValues = VideoMetadataFormValues;
